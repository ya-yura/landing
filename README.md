# Посадочная под заявку

Премиальный русскоязычный статический лендинг услуги создания отдельной посадочной страницы под один конкретный путь клиента.

## Локальный запуск

Проект не требует сборщика и зависимостей. Запустите любой статический сервер из этой папки, например:

```powershell
py -m http.server 4173
```

Откройте <http://localhost:4173/>.

## Структура

- `index.html` — семантическая разметка лендинга и каталог публичных примеров.
- `styles.css` — адаптивная стилизация, состояния фокуса и reduced motion.
- `script.js` — мобильное меню, интерактивные маршруты, фильтры с синхронизацией URL и scroll-reveal.
- `favicon.svg`, `og-cover.svg` — favicon и Open Graph-обложка.
- `robots.txt`, `sitemap.xml` — базовая SEO-настройка.
- `.github/workflows/pages.yml` — публикация содержимого репозитория на GitHub Pages.

## Публикация

После создания GitHub-репозитория `landing-path-20260812` workflow из `.github/workflows/pages.yml` публикует сайт на GitHub Pages.

- Репозиторий: <https://github.com/ya-yura/landing-path-20260812>.
- Демо: <https://ya-yura.github.io/landing-path-20260812/>.

## Проверенные материалы

В каталог включены публичные страницы из брифа. На момент сборки все 18 URL ответили `200 OK`; у 11 ранее использовавшихся адресов заголовки считаны с самих страниц. Визуальные карточки используют нейтральные интерфейсные фрагменты и не приписывают страницам коммерческие показатели.

Основной канал связи — `mailto:sharikov@gmail.com` с темой «Посадочная страница под услугу».

## Допущения и ограничения

- Сроки, результаты, конверсия, лиды и окупаемость не обещаются.
- В карточках проектов сохранены названия и описания из брифа; они показывают тип маршрута или интерфейса, а не подтверждённые бизнес-результаты.
- Страница рассчитана на GitHub Pages и не содержит backend. Основной CTA открывает почтовый клиент пользователя.
